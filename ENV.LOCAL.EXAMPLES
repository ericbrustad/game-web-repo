🧩 Core Environment Variables

(from your project readmes, deployment docs, and .env commits)

# GitHub API
REPO_OWNER=ericbrustad
REPO_NAME=Admin-esxape-ride
GITHUB_TOKEN=ghp_yourgithubapitoken
GITHUB_BRANCH=main
GITHUB_BASE_DIR=apps/admin   # or "" if project root

# Supabase (Game + Admin shared)
SUPABASE_URL=https://<your-project>.supabase.co
SUPABASE_ANON_KEY=<anon-key>
SUPABASE_SERVICE_ROLE_KEY=<service-role-key>

# Game toggle
GAME_ENABLED=0
NEXT_PUBLIC_GAME_ENABLED=0

# Frontend shared URLs
NEXT_PUBLIC_GAME_ORIGIN=https://game.esxaperide.com
NEXT_PUBLIC_DEFAULT_GAME_SLUG=demo
NEXT_PUBLIC_DEFAULT_CHANNEL=published

# Mapbox (used in admin + game-web)
NEXT_PUBLIC_MAPBOX_TOKEN=pk.your-mapbox-token-here

# Vercel settings
NPM_CONFIG_REGISTRY=https://registry.npmjs.org/
NODE_OPTIONS=--dns-result-order=ipv4first
NODE_ENV=production

# Basic Auth (for admin middleware.js)
BASIC_AUTH_USER=Eric
BASIC_AUTH_PASS=someStrongPassword

# Vercel project + deployment tokens (used in /api/change-auth.js)
VERCEL_TOKEN=<vercel-api-token>
VERCEL_PROJECT_ID_OR_NAME=admin-esxape-ride
VERCEL_DEPLOY_HOOK_URL=https://api.vercel.com/v1/integrations/deploy/...

SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
REPO_OWNER=
REPO_NAME=
GITHUB_TOKEN=
GITHUB_BRANCH=
GITHUB_BASE_DIR=
NEXT_PUBLIC_MAPBOX_TOKEN=
NEXT_PUBLIC_GAME_ORIGIN=
NEXT_PUBLIC_DEFAULT_GAME_SLUG=demo
NEXT_PUBLIC_DEFAULT_CHANNEL=published
GAME_ENABLED=0
NEXT_PUBLIC_GAME_ENABLED=0
BASIC_AUTH_USER=
BASIC_AUTH_PASS=
VERCEL_TOKEN=
VERCEL_PROJECT_ID_OR_NAME=
VERCEL_DEPLOY_HOOK_URL=



🕹 Game-Web Side (shared with Admin)

You can mirror these same keys in apps/game-web/.env.local:

NEXT_PUBLIC_SUPABASE_URL=$SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY
NEXT_PUBLIC_MAPBOX_TOKEN=$NEXT_PUBLIC_MAPBOX_TOKEN
NEXT_PUBLIC_DEFAULT_GAME_SLUG=demo
NEXT_PUBLIC_DEFAULT_CHANNEL=published
NEXT_PUBLIC_GAME_ORIGIN=https://game.esxaperide.com
